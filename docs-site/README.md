# chatstrata docs site

Zensical-powered documentation site for chatstrata.

## Build

```bash
uv run --group dev zensical build
```

Run commands from this `docs-site/` directory so Zensical can find
`zensical.toml`.
